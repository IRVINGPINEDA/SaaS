# Local default (no hosts file needed)
BASE_DOMAIN=localtest.me

# Alternative local (no hosts file needed, ip-embedded domain):
# BASE_DOMAIN=127.0.0.1.sslip.io

# EC2 + sslip.io example:
# BASE_DOMAIN=<ELASTIC_IP>.sslip.io
